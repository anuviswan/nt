<UserControl x:Class="Nt.WpfClient.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nt.WpfClient.Views"
             xmlns:vms="clr-namespace:Nt.WpfClient.ViewModels"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--<ItemsControl ItemsSource="{Binding Reviews}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2" BorderBrush="Red">
                        <ContentControl Content="{Binding}" Background="AliceBlue"/>
                    </Border>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->

        <ContentControl x:Name="ReviewItem" Background="AliceBlue"/>
    </Grid>
</UserControl>
