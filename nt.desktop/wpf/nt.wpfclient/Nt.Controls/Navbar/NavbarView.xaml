<UserControl x:Class="Nt.Controls.Navbar.NavbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nt.Controls.Navbar"
             xmlns:localize="clr-namespace:Nt.Utils.Resources.Properties;assembly=Nt.Utils.Resources"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:NavbarViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="gainsboro">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{x:Static localize:Resources.LL_Common_AppTitle}" Padding="5,0,0,0"
               VerticalAlignment="Center"  FontSize="16" FontWeight="Bold" Foreground="Black"/>
        <Ellipse Grid.Column="1" Height="25" Width="25" Fill="Red">
            <!--<Ellipse.Fill>
                <ImageBrush ImageSource="YourImage.png"/>
            </Ellipse.Fill>-->
        </Ellipse>
        <TextBlock Grid.Column="2" Text="{Binding UserName}" HorizontalAlignment="Left" 
                   Padding="5,0,5,0" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
